<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pupil.alnukhba" >
	
	<uses-permission android:name="android.permission.INTERNET"/>
	<uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
	<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
	<uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />   <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launch"
        android:label="@string/app_name"
        android:theme="@style/AppTheme"
		android:resizeableActivity = "true">
		
		
		<activity 
			android:name=".DegreesActivity"
			android:label="@string/app_name">
		</activity>
		<activity 
			android:name=".ShowFees"
			android:label="@string/app_name">
		</activity>
		<activity 
			android:name=".MainSignActivity"
			android:label="@string/app_name">
		</activity>
		<activity 
			android:name=".MainFirstActivity"
			android:label="@string/app_name">
		</activity>

		<activity
			android:name=".ChooseImageActivity"
			android:label="@string/app_name">
		</activity>

		<activity 
				android:name=".InsertActivity" 
				android:label="@string/app_name">
		</activity>
		
		<activity 
			
				android:name=".discripactivity"
				
				android:label="@string/app_name">
	
		</activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
			android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
